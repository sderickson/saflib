# saf-ts-run

```
Usage: saf-ts-run <script.ts> [args...]

Run a TypeScript file with monorepo-standard Node flags.
Loads `.env` from the current working directory when present.
Always enables `--experimental-strip-types`.
```
