[**@saflib/parser**](../index.md)

---

# Function: extractVueSfc()

> **extractVueSfc**(`source`): [`VueSfcSurface`](../interfaces/VueSfcSurface.md)

Extract `<script>` text plus `defineProps` / `defineEmits` / `defineModel`
members as `prop` / `emit` / `model` export entries (syntactic only).

## Parameters

| Parameter | Type     |
| --------- | -------- |
| `source`  | `string` |

## Returns

[`VueSfcSurface`](../interfaces/VueSfcSurface.md)
