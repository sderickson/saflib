import { describe, it, expect } from "vitest";
import * as exports from "example-http";

describe("example-http", () => {
  it("should be defined", () => {
    expect(exports).toBeDefined();
  });
});
