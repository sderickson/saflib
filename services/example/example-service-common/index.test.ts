import { describe, it, expect } from "vitest";
import * as exports from "example-service-common";

describe("example-service-common", () => {
  it("should be defined", () => {
    expect(exports).toBeDefined();
  });
});
