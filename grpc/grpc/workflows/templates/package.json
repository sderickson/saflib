{
  "name": "template-package-grpc",
  "description": "gRPC server for the __service-name__ service",
  "type": "module",
  "exports": {
    ".": "./grpc.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "typecheck": "tsc"
  },
  "dependencies": {
    "@saflib/grpc": "*",
    "template-package-common": "*",
    "template-package-db": "*",
    "template-package-protos": "*",
    "@saflib/monorepo": "*"
  },
  "devDependencies": {
    "@saflib/monorepo": "*",
    "@saflib/vitest": "*"
  }
}
