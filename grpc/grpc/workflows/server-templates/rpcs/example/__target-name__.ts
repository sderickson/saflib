// TODO: Uncomment, make sure types work, and add implementation

// import { templateService } from "@template/file-protos";

// export const handleTemplateFile: templateService.UnimplementedTemplateService["TemplateFile"] =
//   async (call, callback) => {
//     const request = call.request;

//     // Implementation

//     const response = new templateService.TemplateFileResponse({});

//     callback(null, response);
//   };
// };
