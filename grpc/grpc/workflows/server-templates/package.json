{
  "name": "template-package-grpc-server",
  "description": "gRPC server for the __service-name__ service",
  "type": "module",
  "exports": {
    ".": "./grpc.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "typecheck": "tsc"
  },
  "dependencies": {
    "@saflib/grpc": "*",
    "template-package-service-common": "*",
    "template-package-db": "*",
    "@template/file-protos": "*",
    "@saflib/monorepo": "*"
  },
  "devDependencies": {
    "@saflib/monorepo": "*",
    "@saflib/vitest": "*"
  }
}
