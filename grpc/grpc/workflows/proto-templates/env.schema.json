{
  "type": "object",
  "properties": {
    "__SERVICE_NAME___SERVICE_GRPC_HOST": {
      "type": "string",
      "description": "The host on which the gRPC server will listen, or the client will connect."
    }
  },
  "required": ["__SERVICE_NAME___SERVICE_GRPC_HOST"]
}
