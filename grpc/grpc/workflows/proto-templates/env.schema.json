{
  "type": "object",
  "properties": {
    "__SERVICE_NAME___SERVICE_GRPC_PORT": {
      "type": "string",
      "description": "The port on which the gRPC server will listen, or the client will connect."
    }
  },
  "required": ["__SERVICE_NAME___SERVICE_GRPC_PORT"]
}
