[**@saflib/monorepo**](../../../index.md)

---

# Function: isColocatedRootTestFile()

> **isColocatedRootTestFile**(`fileName`, `rootTsFiles`): `boolean`

Root `foo.test.ts` colocated with root `foo.ts` (same stem, not `index`).

## Parameters

| Parameter     | Type                |
| ------------- | ------------------- |
| `fileName`    | `string`            |
| `rootTsFiles` | readonly `string`[] |

## Returns

`boolean`
