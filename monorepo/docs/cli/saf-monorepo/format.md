# saf-monorepo format

```
Usage: saf-monorepo format [options] <filename>

Format a file with Prettier

Arguments:
  filename    File to format with Prettier

Options:
  -h, --help  display help for command
```
