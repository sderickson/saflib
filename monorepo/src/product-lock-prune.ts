/**
 * Product/saflib lock alignment (`saf-monorepo lock-prune`).
 *
 * Implementation lives in `./lock-prune/`. This file re-exports the public API
 * for existing imports (`product-lock-prune.ts`, `@saflib/monorepo` index).
 */
export * from "./lock-prune/index.ts";
