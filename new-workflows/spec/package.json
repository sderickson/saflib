{
  "name": "@saflib/new-workflows-spec",
  "saf": {
    "kind": "spec"
  },
  "description": "HTTP surface + shared business-object schemas for the sqlite-backed workflows engine rewrite. Mounted into dev-site-http as monolith chrome — see new-workflows-http.",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./index.ts",
    "./openapi.yaml": "./openapi.yaml",
    "./operations/*": "./dist/operations/*/index.ts",
    "./schemas/*": "./dist/schemas/*/index.ts"
  },
  "scripts": {
    "build": "saf-specs generate -f openapi.yaml -o dist",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc",
    "generate": "npm exec saf-specs generate"
  },
  "dependencies": {
    "@saflib/openapi": "*"
  },
  "devDependencies": {
    "@saflib/vitest": "*"
  },
  "sideEffects": false
}
