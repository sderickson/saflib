import { describe, it, expect } from "vitest";
import * as exports from "@saflib/new-workflows-db";

describe("new-workflows-db", () => {
  it("should be defined", () => {
    expect(exports).toBeDefined();
  });
});
