[**@saflib/imports**](../../index.md)

---

# Function: isGraphSourcePath()

> **isGraphSourcePath**(`relPosix`): `boolean`

`.ts` / `.tsx` / `.vue` sources parsed for exports and import graph edges.

## Parameters

| Parameter  | Type     |
| ---------- | -------- |
| `relPosix` | `string` |

## Returns

`boolean`
