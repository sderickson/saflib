[**@saflib/imports**](../../../../index.md)

---

# Function: computeExportsMap()

> **computeExportsMap**(`pkgDir`): [`ExportsMap`](../type-aliases/ExportsMap.md)

Compute the heuristic `exports` map for a package.
`index.ts` in a directory maps to `./<dir>` (or `.` at package root).

## Parameters

| Parameter | Type     |
| --------- | -------- |
| `pkgDir`  | `string` |

## Returns

[`ExportsMap`](../type-aliases/ExportsMap.md)
