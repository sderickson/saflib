[**@saflib/imports**](../../../../index.md)

---

# Function: isInternalReference()

> **isInternalReference**(`packageDir`, `refPath`): `boolean`

Paths that stay inside the package (e.g. `./tsconfig.app.json`).

## Parameters

| Parameter    | Type                    |
| ------------ | ----------------------- |
| `packageDir` | `string`                |
| `refPath`    | `undefined` \| `string` |

## Returns

`boolean`
