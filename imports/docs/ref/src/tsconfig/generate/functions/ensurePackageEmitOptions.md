[**@saflib/imports**](../../../../index.md)

---

# Function: ensurePackageEmitOptions()

> **ensurePackageEmitOptions**(`tsconfigPath`): `boolean`

Ensure per-package emit paths and strip legacy noEmit overrides.

## Parameters

| Parameter      | Type     |
| -------------- | -------- |
| `tsconfigPath` | `string` |

## Returns

`boolean`
