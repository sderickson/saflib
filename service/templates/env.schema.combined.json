{
  "type": "object",
  "properties": {
    "@TEMPLATE_SERVICE_HTTP_PORT": {
      "type": "string",
      "description": "HTTP port for the @template/template-file service"
    }
  },
  "required": ["@TEMPLATE_SERVICE_HTTP_PORT"]
}
