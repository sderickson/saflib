{
  "name": "@template/template-file",
  "description": "Service package for @template/template-file",
  "type": "module",
  "scripts": {
    "start": "node --experimental-strip-types ./bin/run.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "healthcheck": "node --experimental-strip-types ./bin/healthcheck",
    "typecheck": "tsc"
  },
  "dependencies": {
    "@saflib/env": "*",
    "@saflib/express": "*",
    "@saflib/links": "*",
    "@saflib/monorepo": "*",
    "@saflib/xstate": "*",
    "@template/template-file-db": "*",
    "@template/template-file-http": "*",
    "@template/template-file-spec": "*"
  },
  "devDependencies": {
    "@saflib/vitest": "*"
  }
}
