{
  "type": "object",
  "properties": {
    "TEMPLATE_FILE_SERVICE_HTTP_PORT": {
      "type": "string",
      "description": "HTTP port for the @template/file-service"
    }
  },
  "required": ["TEMPLATE_FILE_SERVICE_HTTP_PORT"]
}
