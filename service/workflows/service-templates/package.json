{
  "name": "template-package-service",
  "description": "Service package for __service-name__",
  "type": "module",
  "exports": {
    ".": "./index.ts"
  },
  "scripts": {
    "start": "node --experimental-strip-types ./run.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "healthcheck": "node --experimental-strip-types ./bin/healthcheck",
    "typecheck": "tsc"
  },
  "dependencies": {
    "@saflib/env": "*",
    "@saflib/monorepo": "*",
    "template-package-db": "*",
    "template-package-http": "*",
    "template-package-service-common": "*"
  },
  "devDependencies": {
    "@saflib/vitest": "*"
  }
}
