{
  "name": "template-package-service-common",
  "description": "Shared types and utilities for the __service-name__ service",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@saflib/monorepo": "*",
    "@saflib/node": "*"
  }
}
