/**
 * Stub - will be generated as part of the workflow
 */

export interface TemplateFileServiceEnvSchema {}

export const typedEnv = (globalThis.process
  ? process.env
  : {}) as unknown as TemplateFileServiceEnvSchema;
