import { describe, expect, it } from "vitest";
import { run__ServiceName__Cron } from "./cron.ts";

describe("run__ServiceName__Cron", () => {
  it("should be defined", () => {
    expect(run__ServiceName__Cron).toBeDefined();
  });
});
