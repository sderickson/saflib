[**@saflib/express**](../../../index.md)

---

# Function: fetchKratosIdentityById()

> **fetchKratosIdentityById**(`id`): `Promise`\<\{ `identity`: `unknown`; `ok`: `true`; \} \| \{ `ok`: `false`; `status`: `number`; \}\>

Fetches a Kratos identity by id from the admin API.

## Parameters

| Parameter | Type     |
| --------- | -------- |
| `id`      | `string` |

## Returns

`Promise`\<\{ `identity`: `unknown`; `ok`: `true`; \} \| \{ `ok`: `false`; `status`: `number`; \}\>
