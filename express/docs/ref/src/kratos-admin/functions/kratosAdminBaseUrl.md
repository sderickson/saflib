[**@saflib/express**](../../../index.md)

---

# Function: kratosAdminBaseUrl()

> **kratosAdminBaseUrl**(): `string`

Shared Kratos Admin API helpers (identity lookup by id / email).

Uses `KRATOS_ADMIN_API_URL` (default `http://kratos:4434`).

## Returns

`string`
