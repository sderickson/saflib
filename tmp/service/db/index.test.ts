import { describe, it, expect } from "vitest";
import * as exports from "@saflib/tmp-db";

describe("@saflib/tmp-db", () => {
  it("should be defined", () => {
    expect(exports).toBeDefined();
  });
});
