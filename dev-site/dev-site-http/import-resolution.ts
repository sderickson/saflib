/**
 * Resolve import specifiers — canonical home is `@saflib/imports`.
 */
export {
  exportUsedByKey,
  moduleTargetFromImport,
  packageLocalPath,
  resolveRelative,
  stripTsExt,
  type ImportUsedBy,
} from "@saflib/imports";
