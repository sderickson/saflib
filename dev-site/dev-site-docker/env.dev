# @saflib/new-workflows — agent CLI auth for "run"-mode workflows.
# "mock" sentinel (per @saflib/secret-store convention): real values go in a
# gitignored ./.env layered on top (see docker-compose.yaml's env_file order).
ANTHROPIC_API_KEY=mock
CURSOR_API_KEY=mock
