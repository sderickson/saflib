{
  "type": "object",
  "properties": {
    "CLIENT_SUBDOMAINS": {
      "type": "string",
      "description": "Comma-separated list of client subdomains, e.g. 'www,app,auth'. Include an empty string to indicate there's client for the root domain."
    }
  },
  "required": ["CLIENT_SUBDOMAINS"]
}