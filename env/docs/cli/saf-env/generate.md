# saf-env generate

```
Usage: saf-env generate [options]

Generate env.ts. Pass --combined to also generate env.schema.combined.json.

Options:
  -c, --combined  Whether to export the combined schema as well.
  -h, --help      display help for command
```
