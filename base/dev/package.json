{
  "name": "@saflib/base-dev",
  "description": "Docker compose setup for dev environment",
  "type": "module",
  "scripts": {
    "typecheck": "tsc -b",
    "build": "./build-images.sh",
    "dev": "saf-git-hashes && saf-docker prune && saf-docker generate && ./build-images.sh && docker compose --env-file env.dev --env-file .env -f docker-compose.yaml up -V",
    "up": "saf-git-hashes && saf-docker prune && saf-docker generate && ./build-images.sh && docker compose --env-file env.dev --env-file .env -f docker-compose.yaml up --detach -V",
    "down": "saf-docker generate && docker compose --env-file env.dev --env-file .env -f docker-compose.yaml down",
    "dev-site": "saf-docker generate && docker compose -f docker-compose.dev-site.yaml up --build -V",
    "dev-site:down": "docker compose -f docker-compose.dev-site.yaml down"
  },
  "dependencies": {
    "@saflib/dev-tools": "*"
  },
  "sideEffects": false
}
