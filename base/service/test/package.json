{
  "name": "@saflib/base-test",
  "saf": {
    "kind": "test"
  },
  "description": "Shared factories for core service models used in unit tests (SPA, HTTP, SDK fakes). Offshoot-specific helpers live in each offshoot's *-test package.",
  "private": true,
  "type": "module",
  "exports": {
    "./shared/*": "./shared/*.ts",
    "./factories/*": "./factories/*.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -b"
  },
  "dependencies": {
    "@saflib/base-spec": "*",
    "@saflib/monorepo": "*"
  },
  "devDependencies": {
    "@saflib/vitest": "*"
  },
  "sideEffects": false,
  "imports": {
    "#shared/*": "./shared/*",
    "#factories/*": "./factories/*"
  }
}
