/** Load product-wide styles once from each SPA and VitePress theme entry
 *  (Vuetify + design system + utilities). */
import "vuetify/styles";
import "../build/globals.scss";
import "../build/vuetify-overrides.scss";
