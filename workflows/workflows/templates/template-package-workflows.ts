import type { WorkflowDefinition } from "@saflib/workflows";

/** Stand-in for the `template-package/workflows` import inside workflow areas. */
const workflows: WorkflowDefinition[] = [];

export default workflows;
