[**@saflib/workflows**](../index.md)

---

# Variable: PromptStepMachine

> `const` **PromptStepMachine**: `StateMachine`\<`PromptStepContext`, `AnyEventObject`, \{\[`key`: `string`\]: `undefined` \| `ActorRefFromLogic`\<`PromiseActorLogic`\<`unknown`, `NonReducibleUnknown`, `EventObject`\>\> \| `ActorRefFromLogic`\<`PromiseActorLogic`\<[`PromptStepOutput`](../interfaces/PromptStepOutput.md), `PromptStepContext`, `EventObject`\>\>; \}, `Values`\<\{ `noop`: \{ `id`: `undefined` \| `string`; `logic`: `PromiseActorLogic`\<`unknown`, `NonReducibleUnknown`, `EventObject`\>; `src`: `"noop"`; \}; `prompt`: \{ `id`: `undefined` \| `string`; `logic`: `PromiseActorLogic`\<[`PromptStepOutput`](../interfaces/PromptStepOutput.md), `PromptStepContext`, `EventObject`\>; `src`: `"prompt"`; \}; \}\>, \{ `params`: `LogParams`; `type`: `"log"`; \}, \{ `params`: `unknown`; `type`: `"shouldSkip"`; \}, `never`, `"done"` \| `"standby"` \| `"running"`, `string`, [`PromptStepInput`](../interfaces/PromptStepInput.md) & `WorkflowInput`, `NonReducibleUnknown`, `EventObject`, `MetaObject`, \{ `context`: (`__namedParameters`) => `object`; `id`: `"prompt-step"`; `initial`: `"running"`; `output`: (`__namedParameters`) => `object`; `states`: \{ `done`: \{ `type`: `"final"`; \}; `running`: \{ `entry`: `ActionFunction`\<`PromptStepContext`, `AnyEventObject`, `AnyEventObject`, `undefined`, `never`, `never`, `never`, `never`, `never`\>; `invoke`: \{ `input`: (`__namedParameters`) => `PromptStepContext`; `onDone`: readonly \[\{ `actions`: readonly \[`ActionFunction`\<`PromptStepContext`, `DoneActorEvent`\<[`PromptStepOutput`](../interfaces/PromptStepOutput.md), `string`\>, `AnyEventObject`, `undefined`, `Values`\<\{ `noop`: ...; `prompt`: ...; \}\>, `never`, `never`, `never`, `never`\>\]; `target`: `"standby"`; \}\]; `src`: `"prompt"`; \}; `on`: \{ `continue`: readonly \[\{ `guard`: (`__namedParameters`) => `boolean`; `target`: `"running"`; \}, \{ `target`: `"done"`; \}\]; \}; \}; `standby`: \{ `entry`: `ActionFunction`\<`PromptStepContext`, `AnyEventObject`, `AnyEventObject`, `undefined`, `never`, `never`, `never`, `never`, `never`\>; `on`: \{ `continue`: \{ `target`: `"done"`; \}; `maybeContinue`: \{ `guard`: (`__namedParameters`) => `boolean`; `target`: `"done"`; \}; `prompt`: \{ `actions`: readonly \[(`__namedParameters`) => `void`\]; \}; \}; \}; \}; \}\>

Prompts the agent or user to do something. Stops the workflow until the workflow is continued.
