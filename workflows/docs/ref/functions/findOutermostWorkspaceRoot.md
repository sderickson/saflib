[**@saflib/workflows**](../index.md)

---

# Function: findOutermostWorkspaceRoot()

> **findOutermostWorkspaceRoot**(`start`): `string`

Walk up from `start` and return the outermost directory whose package.json
declares `workspaces`.

## Parameters

| Parameter | Type     |
| --------- | -------- |
| `start`   | `string` |

## Returns

`string`
