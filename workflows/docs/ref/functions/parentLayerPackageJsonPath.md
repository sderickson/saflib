[**@saflib/workflows**](../index.md)

---

# Function: parentLayerPackageJsonPath()

> **parentLayerPackageJsonPath**(`parentDir`): `string`

Parent weave host package.json — missing when scaffolding standalone saflib packages.

## Parameters

| Parameter   | Type     |
| ----------- | -------- |
| `parentDir` | `string` |

## Returns

`string`
