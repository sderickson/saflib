import { describe, it, expect } from "vitest";
import * as exports from "@saflib/backup-http";

describe("@saflib/backup-http", () => {
  it("should be defined", () => {
    expect(exports).toBeDefined();
  });
});
