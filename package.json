{
  "name": "@saflib/saflib",
  "description": "Full-featured web application framework using my preferred libraries and services.",
  "scripts": {
    "test": "npm run test --workspaces --if-present --parallel",
    "test:coverage": "npm run test:coverage --workspaces --if-present --parallel",
    "test:global": "vitest run",
    "test:global:watch": "vitest",
    "typecheck": "npm run typecheck --workspaces --if-present --parallel",
    "typecheck:global": "vue-tsc -b"
  },
  "author": "Scott Erickson <sderickson@gmail.com>",
  "license": "MIT",
  "workspaces": [
    "*"
  ],
  "devDependencies": {
    "@saflib/vue-spa": "*"
  },
  "engines": {
    "node": ">=22"
  }
}
