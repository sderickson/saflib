import { describe, it, expect } from "vitest";
import * as exports from "template-package-identity";

describe("template-package-identity", () => {
  it("should be defined", () => {
    expect(exports).toBeDefined();
  });
});
