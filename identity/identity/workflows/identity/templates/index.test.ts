import { describe, it, expect } from "vitest";
import * as exports from "@template/file-identity";

describe("@template/file-identity", () => {
  it("should be defined", () => {
    expect(exports).toBeDefined();
  });
});
