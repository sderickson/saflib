{
  "type": "object",
  "properties": {
    "IDENTITY_SERVICE_GRPC_HOST": {
      "type": "string",
      "description": "The port on which the gRPC server will listen, or the client will connect."
    }
  },
  "required": ["IDENTITY_SERVICE_GRPC_HOST"]
}
