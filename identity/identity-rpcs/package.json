{
  "name": "@saflib/identity-rpcs",
  "description": "Protocol Buffer definitions and TypeScript types for authentication and user management services",
  "type": "module",
  "private": true,
  "exports": {
    ".": "./index.ts"
  },
  "scripts": {
    "clean": "rm -rf dist/*",
    "generate": "npm run clean && ./generate.sh",
    "typecheck": "tsc"
  },
  "dependencies": {
    "@saflib/env": "*",
    "@saflib/grpc-specs": "*"
  },
  "devDependencies": {
    "@saflib/monorepo": "*",
    "@types/google-protobuf": "*"
  }
}
