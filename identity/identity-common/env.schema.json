{
  "type": "object",
  "properties": {
    "IDENTITY_SERVICE_HTTP_HOST": {
      "type": "string",
      "description": "Host URL for the identity service"
    }
  },
  "required": ["IDENTITY_SERVICE_HTTP_HOST"]
}
