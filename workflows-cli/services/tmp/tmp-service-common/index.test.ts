import { describe, it, expect } from "vitest";
import * as exports from "tmp-service-common";

describe("tmp-service-common", () => {
  it("should be defined", () => {
    expect(exports).toBeDefined();
  });
});
