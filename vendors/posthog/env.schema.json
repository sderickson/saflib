{
  "type": "object",
  "properties": {
    "POSTHOG_PROJECT_API_KEY": {
      "type": "string",
      "description": "PostHog project API key for server-side capture. Omit, leave empty, or set to \"mock\" to use the in-memory analytics implementation (no network)."
    },
    "POSTHOG_PROJECT_HOST": {
      "type": "string",
      "description": "PostHog ingest host for server-side capture. Defaults to https://us.i.posthog.com when unset."
    }
  },
  "required": []
}
