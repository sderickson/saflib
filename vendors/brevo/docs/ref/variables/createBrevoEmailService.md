[**@saflib/vendors-brevo**](../index.md)

---

# Variable: createBrevoEmailService()

> `const` **createBrevoEmailService**: (`apiKey`) => `EmailService` = `createEmailService`

Alias matching the Infisical-style vendor factory naming.

Creates a Brevo-backed email service. Pass `"mock"` to record sends in
the shared in-memory mock store without calling the API.

## Parameters

| Parameter | Type     |
| --------- | -------- |
| `apiKey`  | `string` |

## Returns

`EmailService`
