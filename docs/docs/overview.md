# Overview

`@saflib/docs` generates package documentation from TypeDoc, `vue-component-meta` (Vue SFC props/models/emits/slots/exposed), CLI `--help` output, workflow checklists, and env schemas.

Run `npm exec saf-docs generate` in a package to generate a `docs/` folder from types, workflows, and bin commands.
