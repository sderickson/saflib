# saf-workflow checklist

```
Usage: saf-workflow checklist [options] <path-or-id>

Show the checklist for a workflow. Can be called with a workflow ID or a file
   path to a workflow definition.

Arguments:
  path-or-id  Workflow ID or path to workflow file

Options:
  -h, --help  display help for command
```
