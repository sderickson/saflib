# saf-docs generate-all

```
Usage: saf-docs generate-all [options]

Generate documentation for all packages in the monorepo that have a docs
   directory

Options:
  -s, --start-from <package>  Start from a specific package
  -h, --help                  display help for command
```
