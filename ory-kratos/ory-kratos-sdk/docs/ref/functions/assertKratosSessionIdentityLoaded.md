[**@saflib/ory-kratos-sdk**](../index.md)

---

# Function: assertKratosSessionIdentityLoaded()

> **assertKratosSessionIdentityLoaded**(`session`): `asserts session is Session`

Loader guard: session must include identity (required-session query succeeded).

## Parameters

| Parameter | Type      |
| --------- | --------- |
| `session` | `unknown` |

## Returns

`asserts session is Session`
