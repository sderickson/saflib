{
  "type": "object",
  "properties": {
    "KRATOS_HANDLER_HTTP_HOST": {
      "type": "string",
      "description": "Host URL for the Ory Kratos courier callback server (e.g. recipes-monolith:3000)."
    },
    "KRATOS_ADMIN_API_URL": {
      "type": "string",
      "description": "Base URL for the Ory Kratos admin API (e.g. http://kratos:4434). Used for server-side identity lookups.",
      "default": "http://kratos:4434"
    }
  },
  "required": ["KRATOS_HANDLER_HTTP_HOST"]
}
