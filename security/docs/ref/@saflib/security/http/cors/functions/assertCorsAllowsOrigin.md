[**@saflib/security**](../../../../../index.md)

---

# Function: assertCorsAllowsOrigin()

> **assertCorsAllowsOrigin**(`headers`, `allowedOrigin`): `void`

Allowed SPA origin should be echoed exactly on preflight/simple responses.

## Parameters

| Parameter       | Type                           |
| --------------- | ------------------------------ |
| `headers`       | `Record`\<`string`, `string`\> |
| `allowedOrigin` | `string`                       |

## Returns

`void`
