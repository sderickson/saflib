import { describe, it, expect } from "vitest";
import * as exports from "@saflib/secrets-service-common";

describe("@saflib/secrets-service-common", () => {
  it("should be defined", () => {
    expect(exports).toBeDefined();
  });
});
