{
  "type": "object",
  "properties": {
    "SECRETS_SERVICE_GRPC_PORT": {
      "type": "string",
      "description": "The port on which the gRPC server will listen, or the client will connect."
    }
  },
  "required": ["SECRETS_SERVICE_GRPC_PORT"]
}
