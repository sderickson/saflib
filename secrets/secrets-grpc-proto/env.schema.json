{
  "type": "object",
  "properties": {
    "SECRETS_SERVICE_GRPC_HOST": {
      "type": "string",
      "description": "The host on which the gRPC server will listen, or the client will connect."
    }
  },
  "required": ["SECRETS_SERVICE_GRPC_HOST"]
}
