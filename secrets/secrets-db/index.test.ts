import { describe, it, expect } from "vitest";
import * as exports from "@saflib/secrets-db";

describe("@saflib/secrets-db", () => {
  it("should be defined", () => {
    expect(exports).toBeDefined();
  });
});
