openapi: 3.0.0
info:
  title: fixture parent
  version: 1.0.0
paths:
  /widgets/{id}:
    get:
      operationId: getWidget
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: ok
          content:
            application/json:
              schema:
                $ref: "pkg:@fixture/offshoot-spec/openapi.yaml#/components/schemas/Widget"
components:
  schemas:
    LocalThing:
      type: object
      properties:
        widget:
          $ref: "pkg:@fixture/offshoot-spec/openapi.yaml#/components/schemas/Widget"
