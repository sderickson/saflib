# Shared Error Schema. Originates from `@saflib/openapi`.
# Specs should $ref: "pkg:@saflib/openapi/schemas/error.yaml" instead of copying this file.
# TODO:
# - Make it strict - no "error" property, just message
# - Update all errors frontend and backend to use "message"
type: object
properties:
  code:
    type: string
    description: A short, machine-readable error code, for when HTTP status codes are not sufficient.
  message:
    type: string
    description: A human-readable description of the error.
    example: "The requested resource could not be found."
